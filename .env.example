# ═══════════════════════════════════════════════════════════════
# Portal Guru — Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════

# ─── AI Provider: Gemini ──────────────────────────────────────────
# Dapatkan API key gratis di https://aistudio.google.com/apikey
#
# 💡 ANTI RATE LIMIT: Kamu bisa pakai MULTIPLE KEY, pisahkan dengan koma!
#    Sistem akan otomatis memilih key secara round-robin untuk mendistribusikan beban.
#
# Contoh dengan 3 key:
#   VITE_GEMINI_API_KEY=key1,key2,key3
#
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Model Gemini yang digunakan (default: gemini-2.0-flash)
# VITE_GEMINI_MODEL=gemini-2.0-flash

# ─── AI Provider: OpenRouter ──────────────────────────────────────
# Daftar di https://openrouter.ai/keys
#
# 💡 ANTI RATE LIMIT: Kamu juga bisa pakai MULTIPLE KEY dipisah koma!
#    OpenRouter akan merotasi key secara acak.
#
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Atau jika pakai proxy server (production):
# VITE_OPENROUTER_PROXY_URL=/api/openrouter

# ─── AI Model Configuration ───────────────────────────────────────
# Model khusus (opsional — default: auto → gpt-4o-mini)
# VITE_AI_MODEL=openai/gpt-4o-mini

# ─── Server-side (Production) ─────────────────────────────────────
# Untuk production, set juga environment variable di Vercel:
#   OPENROUTER_API_KEY — digunakan oleh api/openrouter.ts (server-side)
#   OPENROUTER_ALLOWED_ORIGIN — daftar origin yang diizinkan (default: guru-cerdas.my.id)
#
# Multi-key juga didukung untuk server-side:
#   OPENROUTER_API_KEY=key1,key2,key3
#   GEMINI_API_KEY=key1,key2,key3

# ─── Supabase (Diperlukan) ────────────────────────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ─── Sentry (Optional) ───────────────────────────────────────────
VITE_SENTRY_DSN=

# ─── Web Push / VAPID (Optional) ─────────────────────────────────
# Generate keys: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=

# ─── Environment ──────────────────────────────────────────────────
VITE_APP_ENV=development
